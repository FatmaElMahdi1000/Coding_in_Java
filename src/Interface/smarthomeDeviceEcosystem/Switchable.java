package Interface.smarthomeDeviceEcosystem;

public interface Switchable {

    //Interfaces:
    //if variable added must be static final with value like: static final k = 1
    //Methods(public and abstract(implicitly) should be implemented by public methods only
    void TurnOn();
    void TurnOff();

}
